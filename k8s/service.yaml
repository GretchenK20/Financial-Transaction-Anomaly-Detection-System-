apiVersion: v1
kind: Service
metadata:
  name: financial-fraud-api-service
  labels:
    app: financial-fraud-api
spec:
  type: NodePort
  selector:
    app: financial-fraud-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
