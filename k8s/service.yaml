apiVersion: v1
kind: Service
metadata:
  name: clinical-risk-api-service
  labels:
    app: clinical-risk-api
spec:
  type: NodePort
  selector:
    app: clinical-risk-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
