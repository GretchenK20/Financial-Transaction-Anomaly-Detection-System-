.env
.venv/
__pycache__/
*.pyc
*.pyo
data/raw/transactions/creditcard.csv
data/financial_anomaly.duckdb
*.duckdb
# Trimmed demo DB (all 492 real frauds + a sampled legit subset, ~2MB) is
# small enough to commit — it's what the deployed API serves /transaction/{id}
# from, since the full 242MB database above is intentionally not committed.
!data/demo_financial_anomaly.duckdb
dbt_project/target/
dbt_project/dbt_packages/
dbt_project/logs/
mlruns/
mlruns.db
*.egg-info/
.pytest_cache/
minikube-darwin-arm64

# Large, fully-regenerable model/agent artifacts (rebuild via training scripts
# / `python agents/fraud_agent.py --build-index`)
models/xgboost/shap_values.parquet
agents/chroma_store/
